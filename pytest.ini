[pytest]
testpaths = tests
addopts = -v --tb=short
timeout = 600

markers =
    smoke: fast read-only tests (no cluster mutation)
    lifecycle: full create/scale/destroy cycles (requires live cluster)
    offline: tests that require no cluster connectivity
